import { Button } from '@heroui/react';
import React from 'react';

const UserRowCTA = ({user}) => {
    return (
        <div className='flex justify-end gap-4'>
        </div>
    );
};

export default UserRowCTA;